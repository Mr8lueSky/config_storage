background {
	path=/home/marblesky/Pictures/wallpapers/space.jpg
}
label {
	text = $TIME
	color = rgb(c3c3c3)
	font_size = 96
	font_family = SF Pro Display
	position = 0, 100
	halign = center
	valign = center
}
input-field {
    monitor =
    size = 20%, 5%
    placeholder_text = 
    fail_text = 
    outline_thickness = 3
    inner_color = rgba(0, 0, 0, 0.0) # no fill

    outer_color = rgb(c3c3c3)
    check_color=rgb(ff0090)
    fail_color=rgb(ff0033)

    font_color = rgb(143, 143, 143)
    fade_on_empty = false
    rounding = 15

    position = 0, -20
    halign = center
    valign = center
}

